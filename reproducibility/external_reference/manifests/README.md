# Manifests

This directory contains template manifests used by the metric-extraction pipeline.

The repository does not include the original packet captures.

Users must adapt the template manifests to the location of their own packet captures before running the scripts.

Required columns:

pcap,source,dataset,profile,host,local_ip,nominal_duration_s,include
